name: Pre Build Release

on:
  pull_request:
    types:
      - reopened
      - opened
      - synchronize
    paths:
        - 'package.json'
        - 'package-lock.json'

jobs:
  test:
    if: startsWith( github.head_ref, 'release/') 
    runs-on: ubuntu-latest
    steps:
    - name: Testing
      run: echo "TESTING"
    